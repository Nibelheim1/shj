# 陪玩小游戏：羊了个羊（Sheep Tile Tower）

> 状态：已实施（prototype v7+，替换先前的翻牌配对）
> 验证：`merge_slice_care_games_test.js` + `h5_minigame_depth_test.js`（随 `npm run test:h5` 执行）

## 1. 玩法

- 玩具牌按**多层塔**堆叠，每座塔只有最上层露出的牌可以点击。
- 点击露头牌进入底部 **7 格槽**；凑满 **3 张相同图案**立即消除并连击加分。
- 槽满且没有三张相同 → 本局失败，按**已消除组数 + 得分**结算。
- 清空整座塔 → mastery 级表现；倒计时结束也按进度/得分结算。
- 所有图标复用庭院已有 `assets/art/match3/play_01..play_10.webp`。

## 2. 难度与奖励

| 档位 | 塔基 | 层数 | 图案 | 三连组 | 重叠偏移 | 时间 | 失败表现上限 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 轻松 | 3×3 | 1 | 6 | 6 | 0.10 | 70s | 0.58 | 教学局，按通关/分数结算 |
| 标准 | 4×4 | 2 | 7 | 14 | 0.16 | 80s | 0.72 | 分数也可推进奖励 |
| 困难 | 5×5 | 3 | 8 | 16 | 0.22 | 90s | 0.84 | **未通关也按得分匹配 B/A 档奖励** |
| 大师 | 6×6 | 4 | 9 | 18 | 0.28 | 100s | 0.84 | 高分失败可拿 A 档奖励 |
| 挑战 | 7×7 | 5 | 10 | 20 | 0.32 | 150s | 0.84 | 独立入口：体力 5，只按分数发素材 |

- 卡片按层偏移绘制并做真实矩形重叠判定：上层牌压住下层超过 10% 面积时，下层不可点击；必须先把上层收进槽中。
- 底部槽统一为 **6 格**；基座随难度收窄，露头选择更少、难度更高。
- 图标池：`play_01, herb_01, tool_01, feed_01, build_01, groom_01, charm_01, treasure_01` 每系列一张，再补 `play_08 / tool_08` 两张高等级图，避免同色系混淆。消消乐、连连看与羊了个羊共用同一跨系列池。
- `validActions = triplesCleared`，与照料结算的有效操作门槛（play ≥ 4 组）衔接。
- 表现分公式：清塔 = 0.85 + 时间效率；未清塔 = 0.08 + (0.45×进度 + 0.55×得分/目标) × (失败上限 - 0.08)。
- 因此困难/大师/挑战即使槽满失败，只要有效消除 4 组以上且得分足够，仍按 B/A 档发放素材；挑战模式沿用 scoreBased 奖励（阈值见 `DATA.careGames.challengeRewards.play`）。

## 3. 防死局

- 生成器按“三张同图案连续叠在同一塔”分组，保证每座塔顶部始终可以完成三消。
- 若场上短暂没有露头牌，引擎会自动重洗剩余牌（最多 3 次）并保留类型直方图。

## 4. 替换范围

- `prototype/merge_slice.html`：加载 `sheep-game.js`，新增 `SHEEP_GAME_ASSET_ROOT`。
- `prototype/js/merge/ui.js`：陪玩入口切到 `SheepGame`，玩法说明与结算文案更新。
- `prototype/js/merge/data.js`：`careGames.difficulties.*.play` 改为塔基/层数/图案/分数目标/失败表现上限；挑战分数阈值重校准。
- `build-dist.js`：dist 不再携带 `memory-game.js`（源码保留为遗留引擎，不进入主流程）。

## 5. 验收

- `merge_slice_care_games_test.js`：塔基/层数/组数、露头判定、三消、槽满失败、高分失败奖励、清塔、超时、取消、挑战。
- `h5_minigame_depth_test.js`：Sheep 800 固定种子、200 种子四档结构、清塔、失败/得分结算、事件与 play 素材。
- 全量 `npm run test:h5`。

---

## 6. 难度 v2（已实施，按主理人两轮拍板修订）

> 拍板一：**仅“轻松”档保证每局可解**；标准及以上不保证可解，允许槽满失败按表现结算。
> 拍板二：**难度提升靠“素材种类”而非“基座大小”**——困难和大师基座与标准一致（3×3），只加种类、悬挂与埋深。

### 6.1 诊断

- 旧版困难/大师塔基 5×5 / 6×6：露头 25 / 36 张，找相同太容易；
- v2 第一轮把困难/大师基座加到 3×4 仍是“加基座”方向，露头反而更多，困难/大师显得更简单；
- 修正：**基座固定 3×3，难度加在种类数（11/12/13 种）与悬挂/埋深/山峰上**。

### 6.2 已实施机制

1. **种类递增**：轻松 7 → 标准 9 → 困难 11 → 大师 12 → 挑战 13 种；图标池扩到 16 张（8 族各 1 张 + 8 张高阶图，按家族多样性每局随机抽样）。
2. **基座不随难度变大**：标准/困难/大师均为 3×3；挑战 4×4（长局）。
3. **悬挂组**：每层 N 个完整组 + 最多 `hangPerLayer` 个悬挂组（2 张本层露头、第 3 张压藏在自己对牌上方）；困难 1 悬挂，大师/挑战 2 悬挂。
4. **埋深**：困难 1 层；大师/挑战 2 层（第三张上方再盖一张，需先揭开）。
5. **山峰基座**：大师/挑战启用，中高侧低，开局露头进一步减少。
6. **轻松唯一保证可解**：整组同层构造（三张同种同时露头）。

### 6.3 最终数值

| 档 | 塔基 | 种类 | 牌数 | 层数 | 开局露头(实测) | 悬挂/层 | 埋深 | 山峰 | 时间 | 槽 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 轻松 | 3×3 | 7 | 7×3=21 | 3 | 7–9 | 0 | — | — | 70s | 5 |
| 标准 | 3×3 | 9 | 9×3=27 | 3 | 7–9 | 1 | 1 | — | 85s | 5 |
| 困难 | 3×3 | 11 | 11×3=33 | 4 | 5–8 | 1 | 1 | — | 95s | 5 |
| 大师 | 3×3 | 12 | 12×3=36 | 4 | 4–8 | 2 | 2 | ✅ | 105s | 5 |
| 挑战 | 4×4 | 13 | 13×6=78 | 5 | 8–11 | 2 | 2 | ✅ | 150s | 5 |

表现分公式、失败上限、照料奖励门槛（≥4 组）、挑战按分奖励均不变。

### 6.4 验收（已通过）

- `h5_minigame_depth_test.js`：200 种子 × 4 档结构契约；仅 easy DFS 真实触摸清塔 + mastery。
- `merge_slice_care_games_test.js`：3×3/11 种结构、槽满失败、高分失败（实测 11 种困难局可消 2–3 组、得分可观）、清盘、超时、取消、挑战。
- 全量 `npm run test:h5` 21/21 PASS。

### 6.5 后续校准

- 5 人实机：轻松首通 ≥90%；困难 30 分钟首通 30–50%；大师 <30%；挑战保持硬核。
- 若困难/大师过难：优先下调 `hangPerLayer` 或埋深；若过易：继续加种类（池内还有 4 张富余）。
