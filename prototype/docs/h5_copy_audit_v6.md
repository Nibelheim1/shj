# H5 玩家文案审计 v6

审计范围：`prototype/merge_slice.html` 静态页面、`prototype/js/merge/ui.js` 动态玩家文案与合成棋盘 DOM。审计目标是让页面只说玩家能理解的山海萌宠体验，不把版本、验证、接入状态或实现细节带到游戏里。

## 已完成（本 Worker）

| 位置 | 原文 | 玩家文案/处理 |
| --- | --- | --- |
| `merge_slice.html` 合成棋盘操作栏 | `整理棋盘` 按钮（`#organize-btn`） | 移除按钮；保留药匣按钮与原有棋盘结构。 |
| `merge_slice.html` `#selection-hint` | `拖动素材到空格可整理，拖到同类同阶素材即可合成；长按查看路线` | `拖动素材到空格放置，拖到同类同阶素材即可合成；长按查看路线`。 |

静态 HTML 文案与 DOM 审计已通过：没有 `organize-btn`、`organize-board` 或 `整理棋盘`。

## `ui.js` 待改清单（交给主 Agent）

以下是当前测试逐行扫出的玩家可见命中。测试会保持失败，直到这些字符串被替换/移除；本 Worker 不修改 `ui.js`。

| 行号 | 当前文案/实现痕迹 | 建议的玩家语气 |
| ---: | --- | --- |
| 445、868、882、1570 | `永久委托`（如“永久委托常驻”“永久委托与第二卷预告”） | `委托会继续刷新`、`常驻委托与第二卷预告`。 |
| 1135、1145 | `主线照料节点 · 永久槽位`、`委托 · 永久槽位` | `主线照料节点 · 固定槽位`、`委托 · 固定槽位`，或直接写 `常驻委托`。 |
| 882 | `真实抽卡、排行和社交不在本轮验证范围内。` | `新的住客会随着疗愈进度陆续来信。` |
| 1166 | `体力中心 · 无广告验证版` | `体力中心 · 轻松玩`。 |
| 1168 | `本轮先验证玩法闭环，真实广告、抽卡、社交与排行榜均未接入。` | `体力只影响生成，疗愈、庭院和小游戏随时可以继续。` |
| 1362 | `难度与奖励公开 · 不会暗调` | `难度与奖励说明`。 |
| 1553 | `无数值结算` | `陪伴记录` 或 `只记录本局表现`。 |
| 1689 | `重置当前 H5 疗愈进度？旧版备份不会被删除。` | `要重新开始这段疗愈旅程吗？之前的记录会留在身后。`（去掉 H5 技术标签） |
| 1724 | `检测到更高版本存档：当前仅预览，不会降级覆盖` | `发现较新的旅程记录，本次只展示，不会改动它。` |
| 1725 | `主存档校验失败，已自动从安全备份恢复` | `旅程记录读取遇到小状况，已从备用记录接回。` |
| 1726 | `旧版进度已无损迁移，原存档仍保留` | `之前的旅程已经接续，原来的记录也会保留。` |
| 326 | 引导句含 `体力为零仍能合成、领取岗位产出、整理棋盘和进行照料小游戏` | 删除“整理棋盘”：`体力为零仍能合成、领取岗位产出和进行照料小游戏`。 |
| 1167 | 体力提示数组的回退项为 `整理棋盘` | 移除该回退项，或改为已有的 `合成`/`照料`/`领取岗位`。 |
| 1081、1095、1638 | `organizeBoard`、`整理棋盘` toast 与 `#organize-btn` 监听 | 删除整理功能与监听；保留拖放/同阶合成路径。 |
| 1395 | `消消乐正在整理棋盘，请稍后再试` | `消消乐正在准备棋子，请稍后再试`。 |

“岗位效果永久生效”等描述是实际成长规则，不属于项目管理话术，可按玩法需要保留。

## 验证

```text
node --check prototype/tests/h5_player_copy_test.js  PASS
node prototype/tests/h5_player_copy_test.js          FAIL（预期：ui.js 尚有 30 个待改命中）
```

测试同时检查：静态 HTML 的可见文本及 `aria-label/title/alt` 等可见辅助文案、`#organize-btn` DOM、`ui.js` 字符串与合成整理标识。主 Agent 清理完 `ui.js` 后再次运行即可得到全绿结果。
