# 微信小游戏版 · 2D 渲染能力升级说明

> 本轮改动**只发生在 `wechat/` 目录**，`prototype/` 原型代码与资源完全未动。
> **未新增任何美术资源**，全部用程序化绘制 + 已有素材实现，主包仍为 **3.1MB**（微信 4MB 上限内）。

---

## 一、场景分层 & 画面融合

`renderer.js` 的 `_draw` 从"贴一张背景图 + 叠图片"重构为完整分层管线：

```
1. _drawScene      天空渐变(昼夜) → 远景视差山 → 中景树林 → 地平线薄雾 → 庭院地面(裁剪到地平线下)
2. _drawShadowPass 独立阴影层
3. _drawEntities   建筑 / 装饰 / 神兽 统一入队按 y 深度排序
4. _drawForeground 近景草丛(近大远小 + 视差 + alpha 渐变)
5. 粒子 / 特效
6. Grader.grade    全屏统一调色
7. 昼夜色调 + 夜间径向暗角
8. UI
```

**融合关键**：地平线薄雾把远景与地面软过渡；地面用 `ctx.rect + clip` 裁剪到地平线下方，让天空真正露出来，不再是"一张图糊满屏"。

原先 `bg_far / bg_mid / bg_near` 三个背景层其实是空壳（图片从未加载，永远走灰色兜底），现在由程序化远/中景真实填充。

## 二、图层深度排序

`_drawEntities` 把建筑、装饰、神兽全部塞进同一个 `renderQueue`，按脚底 y 坐标 `ry` 排序后绘制 —— 角色走到建筑前面就在前面，走到后面就被遮挡。取代了之前"把建筑拆成上下两半硬凑遮挡"的临时方案。

## 三、精灵阴影（新模块 `src/render/shadow.js`）

| API | 用途 |
|---|---|
| `softShadow` | 角色/装饰脚下椭圆软投影（径向渐变 + `multiply`） |
| `contactShadow` | 建筑底部紧实接触阴影 |
| `directionalShadow` | 带光照方向偏移的投影 |

阴影绘制在**独立图层**（实体之下），并按 `_depthScale` 随远近缩放，角色跳跃/呼吸时阴影会跟着缩放变淡，有了"离地感"。

## 四、骨骼 / 序列帧动画（新模块 `src/render/animator.js`）

**序列帧**：`Clip(frames, {fps, loop, pingpong})` + `Animator` 状态机，`makeBeastAnimator(def)` 自动为每只神兽生成 idle(5fps) / walk(8fps) / work(4fps) / care(6fps) 四套动画，按状态切换。
> 之前是"每帧随机抽一张图"，现在是真正按 fps 顺序播放。验证：九尾狐 60 步内稳定循环出 6 个不同帧。

**骨骼**：`Bone` 实现 2D 父子变换合成（局部缩放偏移 → 按父级旋转 → 累加），`Rig` 组装 `根骨 → 身体骨 → 灵尾子骨链`。身体骨由呼吸/行走/行为共同驱动，九尾狐的 3 条灵尾作为子骨扇形散开、尾尖独立摆动（`drawSpiritTails` 用 `lighter` 混合画渐隐飘带）。纯 Canvas 2D，无 DOM 依赖。

## 五、颜色统一调色（新模块 `src/render/grader.js`）

- `grade()`：全屏统一色温 —— `multiply` 压暖/冷 + `screen` 提亮，让所有图层落到同一套色调下，消除"素材各画各的"的割裂感。
- `tint(img, id, rgb, alpha)`：**逐实体染色**，用离屏画布 + `source-atop` 实现，结果按 `id|key` 缓存。
- `stateTint()`：生病→绿、疲劳→蓝、脏污→淡蓝。

> ⚠️ 重要坑：微信小游戏 Canvas **不支持 `ctx.filter`**。原代码用 `if (this._filterOK && ...)` 包裹滤镜，而 `_filterOK` 恒为 false —— 真机上所有染色/灰度**全部静默失效**。现在改成离屏合成，与滤镜无关，真机可用。

---

## 修复的 Bug

1. `Rig.bodyTransform()` 返回 `{rot, sx, sy}`，但 renderer 读的是 `wrot / wsx / wsy` → `undefined` → 真机 `ctx.scale(undefined, undefined)` 直接崩。已改为返回 `{x, y, wrot, wsx, wsy}`。
2. `_smoke2.js` 的 mock context 缺 `rect()` / `clip()`，新的地面裁剪逻辑会报 `ctx.rect is not a function`。已补齐。

## 验证结果

| 项目 | 结果 |
|---|---|
| `node --check` × 5 文件 | 全部通过 |
| `_smoke2.js` 冒烟测试 | **22 PASS / 0 FAIL** |
| 序列帧播放 | 60 步内出现 6 个不同帧 ✅ |
| Rig 骨骼变换 | 有限值 = true，tails = 3 ✅ |
| Grader 离屏染色 | 返回有效离屏画布 ✅ |
| 30 帧分层渲染 | 无异常 ✅ |
| 主包体积 | **3.1MB** / 4MB ✅ |

## 后续可扩展

当前骨骼与远/中景均为**程序化实现**（因为要守住 4MB 主包上限）。如果后续愿意上真实素材：
- 远景/中景换成透明 PNG 图层 → 视差效果更自然
- 神兽拆成头/身/四肢/尾多部件 PNG → 挂到现有 `Bone` 上即可获得真骨骼动画，`Rig` 结构无需改动
- 建议走**分包**加载，避免主包再次超限
